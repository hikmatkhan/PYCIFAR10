#wandb: Creating sweep from: sweep.yaml
#wandb: Created sweep with ID: uupcr1ll
#wandb: View sweep at: https://wandb.ai/hikmatkhan-/uncategorized/sweeps/uupcr1ll
#wandb: Run sweep agent with: wandb agent hikmatkhan-/uncategorized/uupcr1ll
#  3245203

program:
    main.py
method: grid
metric:
  name: test_accuracy
  goal: maximize

parameters:
  min-lr:
    value: 1e-05
  max-lr:
    value: 0.1
  optimizer:
    values: ["SGD", "Adam", "RMSprop", "amsgrad", "Adadelta", "Adagrad", "Adamax"]
  wand-project:
    value: "PYCIFAR10"
  username:
    value: "hikmatkhan-"
  wandb-log:
    value: 1
  data:
    value: "./data"
  dataset:
    value: "CIFAR10"
  train-aug:
    values: [0, 1]
  epochs:
    value: 256
  batch-size:
    values: [256]
  model-name:
    values: ["resnet18"]
  lr:
    values: [0.001]




#python -u $LEARNERPATH/main.py --outdir $OUTDIR --dataset CIFAR10 --train_aug --gpuid $GPUID --repeat $REPEAT --optimizer Adam --epochs $EPOCHS --batch_size $BATCH_SIZE --model_name resnet18 --lr 0.01            | tee ${OUTDIR}/resnet18.log
